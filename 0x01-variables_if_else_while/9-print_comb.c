#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int num = 0;
	int comma = 44;
	int space = ' ';

	for (num = 48; num <= 57; num++)
	{
		putchar(num);
		if (num2 < 57)
		{
		putchar(comma);
		putchar(space);
		}
	}
	putchar ('\n');

	return (0);
}
