/**
 * op_add - operations
 *@a: is a pointer to char
 *@b: is a pointer to char
 *Return: NULL
 */


int add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub - operations
 *@a: is a pointer to char
 *@b: is a pointer to char
 *Return: NULL
 */


int sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul - operations
 *@a: is a pointer to char
 *@b: is a pointer to char
 *Return: NULL
 */

int mul(int a, int b)
{
	return (a * b);
}


/**
 * op_div - operations
 *@a: is a pointer to char
 *@b: is a pointer to char
 *Return: NULL
 */
int div(int a, int b)
{
	return (a / b);
}
/**
 * op_mod - operations
 *@a: is a pointer to char
 *@b: is a pointer to char
 *Return: NULL
 */

int mod(int a, int b)
{
	return (a % b);
}
